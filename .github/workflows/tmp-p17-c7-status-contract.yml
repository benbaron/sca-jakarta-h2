name: Temporary P17-C7 status contract correction

on:
  push:
    branches:
      - codex/P17-C7-fixed-asset-lifecycle

permissions:
  contents: write

jobs:
  publish:
    if: github.actor != 'github-actions[bot]'
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Apply status contract correction
        run: |
          python .tmp/p17_c7_fix_status_contract.py
          git diff --check
      - name: Publish correction and remove temporary artifacts
        run: |
          rm -f .tmp/p17_c7_fix_status_contract.py .github/workflows/tmp-p17-c7-status-contract.yml
          rmdir .tmp 2>/dev/null || true
          git config user.name 'github-actions[bot]'
          git config user.email '41898282+github-actions[bot]@users.noreply.github.com'
          git add -A
          git diff --cached --check
          git commit -m 'P17-C7: preserve disposed status validation contract'
          git push origin HEAD:codex/P17-C7-fixed-asset-lifecycle
