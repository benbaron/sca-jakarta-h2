name: Temporary P17-C7 implementation publisher

on:
  push:
    branches:
      - codex/P17-C7-fixed-asset-lifecycle

permissions:
  contents: write

jobs:
  publish:
    if: github.actor != 'github-actions[bot]'
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Apply reviewed fixed-asset lifecycle changes
        run: |
          python .tmp/p17_c7_impl.py
          git diff --check
      - name: Publish product commit and remove temporary artifacts
        run: |
          rm -f .tmp/p17_c7_impl.py .github/workflows/temp-p17-c7-publisher.yml
          rmdir .tmp 2>/dev/null || true
          git config user.name 'github-actions[bot]'
          git config user.email '41898282+github-actions[bot]@users.noreply.github.com'
          git add -A
          git diff --cached --check
          git commit -m 'P17-C7: govern fixed-asset status lifecycle'
          git push origin HEAD:codex/P17-C7-fixed-asset-lifecycle
