# Interface operation matrix

Status: P00 inventory of current main, updated through active P16-S17 end-to-end closure. This document records visible operations and data authority so later phases can replace placeholders without rescanning the whole UI.

## Scope and evidence

- `AppPanelId` retains stable identifiers, but `LEDGER_REGISTER` and `TXN_EDITOR` are retired compatibility aliases normalized to canonical `JOURNAL_PANE`.
- `PanelHost` creates one reusable workspace tab per canonical destination. Requests for either retired P03 alias select the same Journal tab. Selecting an already-open tab invokes that panel's `onPanelShown()` refresh hook, so imported or otherwise externally changed H2 data is re-queried without closing the tab.
- `NavigationPane` exposes one **Journal** destination under Accounting rather than separate Ledger Register, Transaction Editor, and Inspect Journal items.
- `PanelFactory` routes the Journal destination to `JournalWorkspaceCompliancePanel`, which delegates all accounting behavior to `JournalWorkspacePanel` and applies the company UI formatting/state/layout contract.
- `PanelFactory` also applies `CompanyTableStateBinder` at the production composition boundary. Every table on every canonical route is unconstrained, sortable, resizable, reorderable, and registered to H2 company-owned order/width/sort state; Journal, Funds, and Company Admin retain their richer existing H2 state owners.
- The stable `SETTINGS` destination now opens `AdministrationPanel`, which hosts Preferences, Company Admin, and User Admin tabs without introducing additional shell identifiers or a second administration framework.
- Production global commands route from menu, toolbar, and installed accelerators through `GlobalCommandRegistry`, `ProductionWorkspaceWindow`, and `PanelHost` to explicit `AppPanel.commandCapabilities()` and handled/not-handled results. Standard JavaFX text controls retain native Copy/Paste behavior because the workspace does not intercept those accelerators.
- `UiServiceRegistry` creates JPA-backed lookup/admin/report, reconciliation-workspace, and period-close-range services, plus compatibility JDBC repositories for legacy run records and approval audit.
- Search evidence: `UiWorkspaceDataStore` and its temporary bank-transaction staging are removed in P15-S6-C4. The generic Import/Export Jobs panel, route, destination, and session job list were removed in P13-S1; the former top-level Schedules panel, asset/depreciation runbook sidecars, and inventory runbook sidecar were removed in P07/P08-S1/P09-S1.

## Global commands

| Command/source | Visible control | Query source | Write source | Survives restart | H2 authoritative | Placeholder/sidecar risk | Owning phase |
|---|---|---|---|---|---|---|---|
| New | File menu, toolbar, `Ctrl+N` | active `AppPanel.commandCapabilities()` | declared panel `onNew` only | panel-dependent | panel-dependent | disabled with an explanation when undeclared; an empty default cannot report success | P01/P03/P16-S12 |
| Save | File menu, toolbar, `Ctrl+S` | active `AppPanel.commandCapabilities()` | declared panel `onSave` only | panel-dependent | panel-dependent | disabled with an explanation when undeclared; thrown failures return `handled=false` | P01/P02/P03/P16-S12 |
| Copy/Paste | focused standard JavaFX text control | JavaFX focus owner and selection | native text-control edit behavior | no accounting persistence | no | no workspace-level `Ctrl+C`/`Ctrl+V` interception or empty panel delegate | P16-S12 |
| Validate | Journal-local command path | canonical Journal editor state | Journal validation only | no accounting persistence | no | typed `POST_VALIDATE`; supported only by canonical Journal and its aliases | P03/P16-S12 |
| Find/command palette | none in production | none | none | no | no | `Ctrl+F`, `Ctrl+K`, and `Ctrl+G` are not installed or advertised by production Help | P16-S12 |
| Close All Tabs | Workspace menu, `Ctrl+Shift+W` | open/dirty workspace tabs | shell tab lifecycle | no accounting persistence | no | shell-owned; prompts before discarding dirty closable tabs | P01/P16-S12 |
| Close Inspector | `Esc` | inspector visibility | shell view state | no accounting persistence | no | shell-owned and registered with the other installed shortcuts | P01/P16-S12 |
| Legacy Import/Export actions | main-window file actions | COA services plus explicit durable bank/SCLX workspaces | COA import services; bank menu routes to exact-scope Import Preview rather than direct staging | yes for committed domain facts | yes for committed domain facts | no session bank staging or generic job log; legacy orchestration helpers remain non-production compatibility code | P05/P13/P15 |
| Database wizard/switch | production File actions and Dashboard recovery surface | `DatabaseLocationService`, `DatabaseMigrationService`, `DatabaseSessionController`, `UiServiceRegistry` | prepared H2/JPA/service bundle plus authoritative target-company resolution through typed `DatabaseRecoveryCommand` dispatch | yes | yes after successful prepared-session activation | dirty workspaces are confirmed before switching; migration/service/company validation completes before path/company state changes; failure retains the prior services, company, displayed path, and records; no automatic destructive repair | P01/P12/P13/P16-S5 |

### Production global-command capability matrix

`Close All Tabs` and `Close Inspector` are shell-owned and remain available independently of the active panel. Panel-owned capabilities are exact:

| Destination | New | Save | Validate | Notes |
|---|---:|---:|---:|---|
| Dashboard | yes | no | no | New opens the canonical Journal entry workflow. |
| Journal (`JOURNAL_PANE`, `LEDGER_REGISTER`, `TXN_EDITOR`) | yes | yes | yes | Both retired identifiers normalize to the same Journal panel and capability set. |
| Banking, Asset Register, Inventory, Chart of Accounts, Funds | yes | yes | no | Commands invoke the destination's real editor operations. |
| Budget Editor | no | yes | no | Version creation remains an explicit panel-local operation. |
| Administration — Preferences | no | yes | no | Capabilities follow the selected inner tab. |
| Administration — Database Transfer | no | no | no | Transfer commands remain explicit panel-local operations. |
| Administration — Company Admin or User Admin maintenance tabs | yes | yes | no | Selection changes notify the shell immediately; User Admin Authentication exposes neither command. |
| Budget vs Actual, Depreciation Runs, Reconciliation, Period Close, Import Preview, Audit History, Bank Transactions, Report Library, Diagnostics, Help | no | no | no | Read/run/import/report operations stay panel-local; no global command is enabled as a placeholder. |

## Delete operation rule

Do not add disabled placeholder Delete buttons. A durable-record panel may expose a real Delete action only when the authoritative service supports the action and protection rules. Otherwise, explain non-deletable records through status/help text, inactive/disposed workflow copy, or documentation rather than as a disabled button.

## Completed-phase UI design-rule updates

`doc/ui_design_rules.md` applies to every panel listed below, including panels delivered by completed phases. For future inventory passes, record whether each table-bearing panel has sortable/resizable/reorderable columns, per-company saved table state, vertical and horizontal scroll bars, a split-pane boundary from surrounding data, company-preference money/date formatting, and whether Delete is a real supported operation. Any noncompliance found in a completed phase becomes a focused corrective slice rather than a wholesale phase reopening.

P14-S1 adds a production-root route smoke under a disposable migrated H2 database and makes the table interaction/state portion of that contract universal. Banking and Inventory no longer write table layout to Java Preferences. Panel-specific split layout, scrolling, company formatting, and dirty-state repairs remain explicitly assigned to P14-S2 and P14-S3.

P14-S2 adds table-over-editor split regions, independently scrollable forms, company-owned divider positions, and snapshot-based dirty-state protection to Asset Register, Banking, Budget Editor, Chart of Accounts, Preferences, and User Admin. The Administration hub now reports unsaved changes in every child tab, including tabs that are not selected. Financial display formatting and remaining data-view layout corrections remain P14-S3.

P14-S3 routes visible money and dates in Dashboard, Budget vs Actual, Depreciation Runs, Inventory, Reconciliation, Period Close, Audit History, and Bank Transactions through active-company `CompanyUiFormat`. It removes scoped constrained table policies and adds H2 company-owned divider state to the multi-region financial views, including top/bottom table regions for Depreciation Runs and Import Preview. Import Preview has no money/date columns in its current COA staging surface, but its tables and split layout follow the shared contract.

P14-S4 aligns Help and the production Destinations menu with the canonical navigation contract. Help is independently scrollable, links only to authoritative existing repository paths, and reports browser-launch failures with a copyable address. The production route smoke now requires the exact canonical destination set, excludes Schedules, and proves Ledger Register/Transaction Editor compatibility requests reuse the one Journal workspace.

## Production preference consumers

P16-S11 makes `PreferenceConsumerMatrix` the executable inventory behind this documentation. Enabled Settings values have these production consumers; compatibility-only values remain visible as disabled explanatory text rather than editable controls.

| Saved value | Scope | Production consumer | Lifecycle |
|---|---|---|---|
| theme | user machine | `ProductionWorkspaceWindow` theme class and production stylesheet | immediate after Apply/Save; restored at startup |
| unified native decoration | user machine | `MainApp` `StageStyle` | restart required |
| remember window state | user machine | `MainApp` top-level size/position/maximized state and `ProductionWorkspaceWindow` shell dividers | restart-time restore; disabled state clears saved geometry/dividers |
| correction method | desktop session | unified Journal Delete/Reverse choice | subsequent Journal correction action |
| closed-period policy / require reason | desktop session | `PeriodCloseRunsPanel` controls and `PeriodCloseRangeService` command | whenever Period Close is shown |
| confirm entered-transaction deletion | desktop session | unified Journal direct-delete prompt | subsequent direct delete; service protections remain mandatory |
| period start day | desktop session | `ActivePeriodContext` and toolbar accounting-period selection | subsequently selected periods |
| currency symbol / money format / date format | company H2 | `CompanyUiFormat` and production views/editors | active company |
| default privilege | compatibility only | none | disabled; not authentication or authorization |
| default reopen scope | compatibility only | none | disabled; calculated/custom range authority has no session reopen-scope mode |

## Panel matrix

| `AppPanelId` | Panel/class | Visible controls | Query source | Write source | Survives restart | H2 authoritative | Dependencies | Simulated/placeholder/sidecar behavior | Missing work | Owning phase |
|---|---|---|---|---|---|---|---|---|---|---|
| `DASHBOARD` | `DashboardHomePanel` | KPI cards, report/action buttons, tables | `UiServiceRegistry.dashboardQuery`, reports, fund balances | navigation only | yes for queried data | yes for queried data | dashboard query/report/fund services | active-company money/date formatting and H2 divider/table state; no second dashboard data path | final P14 desktop sweep | P01/P14 |
| `JOURNAL_PANE` | `JournalWorkspaceCompliancePanel` delegating to `JournalWorkspacePanel` | one grouped transaction journal; authoritative `Not bank`/`Uncleared`/`Cleared`/`Mixed` summary; filters; New Entry, Edit Selected, Save Entry, Delete/Reverse, Refresh; editable entry-line table with read-only bank state/cleared date and reconciliation drill-through; additional details; persisted supplemental-detail tabs; overall editor scrollbar; draggable resize bars and dedicated table split regions | `TransactionEntryService.search/load` including `TxnSplit.bankCleared`, `bankClearedOn`, and exact native reconciliation-session projection; transaction reference-data service; `CompanyUiPreferencesService`; desktop correction/delete-prompt preferences | `TransactionEntryService.enter/update`; `TransactionCorrectionService.delete/reverse`; persisted `txn_supplemental_line` rows; company-owned money/date/table/divider preferences/state | yes | yes for transaction header, lines, supplemental details, cleared facts, and company UI preferences/state | canonical `Txn`/`TxnSplit`, `bank_reconciliation_session`/`bank_reconciliation_match`, `txn_supplemental_line`, account/fund/budget/activity/merchant/counterparty choices, `company_ui_preference`, `company_ui_state`, `period_close_range` | donor `Journal*` classes are design reference only; the direct-delete preference controls only its prompt, while reversal remains explicit and service protections remain authoritative; Journal renders service-owned cleared facts and never mutates them | owner P16-S11 preference-consumer verification plus retained P16-S10 cleared-state verification | P02/P03/P06/P10/P16-S10/P16-S11 |
| `LEDGER_REGISTER`, `TXN_EDITOR` | retired compatibility aliases | no separate navigation item or independent tab | normalized to `JOURNAL_PANE` | normalized to the same Journal operations | same as Journal | same as Journal | `AppPanelId.canonical`, `PanelHost`, `DrillThroughCoordinator` | preserved only so existing drill-through and saved destination references continue to work | remove only in a deliberate compatibility-breaking release | P03 |
| `BANKING` | `BankingPanel` | bank/configured-account editors, durable review summary, Import Bank Statement and Review / Export Statements actions | `BankConfigurationService`, `AccountLookupService`, `BankReviewQueryService` | `BankConfigurationService`, `AccountAdminService` for auto-created BANK/DEBIT/CASH accounts; navigation to exact-scope import/export | yes | yes | `Bank`, `CompanyBankAccount`, chart `Account`, durable review facts | no sidecar ledger; deactivate preserves statement/reconciliation history | integrated all-format desktop verification remains in P15-S8 | P05/P15-S6/P15-S7/P15-S8 |
| `BUDGET_EDITOR` | `BudgetEditorPanel` | fiscal-year version selector, category budget table, amount editor, explicit New Draft, Create Revision, Save Draft Amount, Activate Version | `BudgetPlanService`, `FiscalPeriodRange`, active budget categories, shell-selected accounting period | explicit draft creation/revision, exact selected-draft line replacement, selected-draft activation | yes, H2 | yes for normalized budget plans/lines | company fiscal settings, `budget_plan`/`budget_line`, budget category lookup | reload never creates or hides a draft; active revisions copy governed lines before editing | owner P16-S6 desktop acceptance | P04/P16-S6 |
| `BUDGET_VS_ACTUAL` | `BudgetVsActualPanel` | active budget variance table, run/refresh | `BudgetPlanService.activeVariance(FiscalPeriodRange)` from company fiscal settings and shell-selected accounting period | none | yes for queried H2 data and divider state | yes for active budget, fiscal-range actual ledger data, and UI state | active `budget_plan`/`budget_line`, canonical ledger actuals, company fiscal settings, company UI preferences/state | neutral state when no active budget version exists; company-formatted money; no wall-clock/calendar-year substitution | owner P16-S6 desktop acceptance | P04/P14/P16-S6 |
| `ASSETS_REGISTER` | `AssetsRegisterPanel` | H2 asset table and form, lifecycle-history table, save/new, frozen Sale/Retirement/Impairment preview-confirm, domain reversal, Ledger drill-through | `FixedAssetService`, canonical `TransactionEntryService`/`TransactionCorrectionService`, account/fund lookup services | `FixedAssetService.create/update/previewLifecycleEvent/recordLifecycleEvent/previewLifecycleReversal/reverseLifecycleEvent` | yes | yes | `fixed_asset`, `fixed_asset_lifecycle_event`, canonical `txn`/`txn_split`, `audit_event`, chart accounts, funds | `DISPOSED` is not directly editable; lifecycle service revalidates under lock and commits status/event/ledger/audits atomically; linked transactions reject generic Journal mutation; S15 reports query the same facts | owner P16-S14 desktop acceptance retained; S15 reporting verified in Report Library | P08/P16-S14/P16-S15 |
| `DEPRECIATION_RUNS` | `DepreciationRunsPanel` | depreciation basis table, run date/notes, run monthly depreciation, completed run table | `FixedAssetService` | `FixedAssetService.runMonthlyDepreciation` validates before mutation and atomically creates the canonical `Txn`/`TxnSplit`, linked `fixed_asset_depreciation_run`, portable identities, and factual `AuditEvent` in one caller-owned JPA transaction | yes | yes | `fixed_asset`, `fixed_asset_depreciation_run`, canonical `Txn`/`TxnSplit`, `audit_event`, closed-period and reconciliation protection | old depreciation runbook removed; the run command is disabled while active and refreshes authoritative persisted state after commit or rollback | richer period batching and report integration | P08/P16-S1 |
| `INVENTORY` | `InventoryPanel` | inventory item table, movement-history table, item form subpanel opened by New Item/Edit Selected, account/fund selectors, governed receipt/issue/adjustment preview-confirm actions, financial reversal, Ledger drill-through | `InventoryService`, canonical `TransactionEntryService`/`TransactionCorrectionService`, account/fund lookup services | `InventoryService.create/update/previewMovement/recordMovement/previewMovementReversal/reverseMovement` | yes | yes | `inventory_item`, `inventory_movement`, canonical `txn`/`txn_split`, `audit_event`, chart accounts, funds | P16-S9 atomically synchronizes quantity and ledger value, explicitly labels zero-value nonfinancial movements, protects close/reconciliation boundaries, and preserves SCLX historical restore without duplicate posting; S15 reports query the same facts | S15 owner Report Library desktop acceptance | P16-S9/P16-S15 |
| `RECONCILIATION_RUNS` | `ReconciliationRunsPanel` | configured-account/session selectors, start/load/reload, Manual Entry, **Import Bank Statement…** navigation, auto-match, exact match/unmatch, mark cleared, factual difference explanation, Save Unresolved, Finalize, explicit successor controls | `BankReconciliationWorkspaceService` authoritative snapshots over configured bank account, durable `BankStatementLine` review facts, canonical `Txn`/`TxnSplit`, reconciliation sessions/matches, and audit facts | reconciliation mutations through `BankReconciliationWorkspaceService`; manual statement fact through `BankStatementManualEntryService` in its caller-owned transaction; external file import writes only through canonical Import Preview review services | yes | yes | `bank_reconciliation_session`, `bank_reconciliation_match`, durable bank-review facts, canonical `Txn`/`TxnSplit`, configured `CompanyBankAccount`, `audit_event`, Import Preview navigation context | no reconciliation-local CSV/OFX/QIF parser or imported-file persistence; reconciliation-origin Import Preview is locked to the exact configured account and reloads H2 facts after commit; finalized sessions remain read-only | owner desktop verification of P16-S4 navigation, exact-account lock, return/reload, failure/cancel behavior, and absence of QIF | P06/P16-S3/P16-S4 |
| `PERIOD_CLOSE_RUNS` | `PeriodCloseRunsPanel` | calculated active-month or custom date range, Close Range, Reopen Selected, Refresh, close-range table, factual event-history table, session-defaulted reopen policy/reason controls, factual editable local actor | `PeriodCloseRangeService.listRanges/listEvents`; active desktop-session closed-period preferences | `PeriodCloseRangeService.closeRange/reopenRange` | yes | yes; `period_close_range`, `period_close_event`, and `audit_event` are H2-backed | active company, active period, `ClosedPeriodPolicy`, canonical transaction services | no approval/rejection, fictional actor, privilege gate, or run-status controls; the older run-record repository remains compatibility-only and is not used by this panel | owner P16-S11 preference-default and factual-actor verification | P10/P16-S11 |
| `IMPORT_PREVIEW` | `ImportPreviewPanel` | SCLX/COA controls plus configured-account/profile selectors, explicit CSV-profile save, OFX/QFX, mapped-CSV, and normalized-bank-CSV preview, original/normalized rows, account confirmation, actor, atomic commit, bounded progress, and Cancel Preview | fixed-scope SCLX services; `CoaCsvImportService`; `BankStatementReviewService`, `BankCsvReviewService`, `NormalizedBankCsvReviewService`, `BankCsvMappingProfileService`, `BankConfigurationService` | exact successful SCLX or bank preview commits through its canonical one-transaction service after confirmation; COA CSV **Commit Accepted COA Rows** commits all accepted account writes, `COA_CSV` identities, and one factual operation audit through `CoaCsvImportService` plus the caller-owned `AccountAdminService` seam in one JPA transaction; P15 COA JSON remains owned by `ChartOfAccountsJsonImportService` | yes for committed facts/profiles/accounts | yes | bounded secure parsers, frozen source SHA-256, active company, target chart/fingerprint, exact accepted/rejected rows, configured account, caller-owned services, transient `InterchangeTaskController` | no direct bank staging or ledger creation and no row-by-row COA durability; cancelled previews publish no result or audit; cancellation locks when a durable commit starts; source/company/chart/target drift requires preview again; any COA row failure rolls back the complete batch and reports zero committed counts | P16-S2 owner desktop verification plus final P15 laptop-width/progress verification | P15-S5/P15-S6/P15-S7/P15-S8/P16-S2 |
| `APPROVAL_AUDIT` | `ApprovalAuditPanel`, visible as Audit History | action/entity/actor/date filters, Refresh/Reset, factual event table, read-only Before/After/Reason detail pane | company-scoped `AuditHistoryService` projections over `AuditEvent` | none from panel | yes | yes; `audit_event` plus company-owned table/divider state | active `Company`, JPA `AuditEvent`, `CompanyUiFormat`, company UI state | stable `APPROVAL_AUDIT`/class name are navigation compatibility only; legacy `approval_audit_record` is not queried or blended into factual history | owner desktop verification of factual rows, filters, read-only details, company isolation, restart, and layout state | P12/P14/P16-S7 |
| `BANK_TRANSACTIONS` | `BankTransactionsPanel` | company-scoped durable review table, explicit **Create Transaction from Reviewed Row…**, matched/accepted ledger drill, independently scrollable configured-account/date scope, explicit Export Bank CSV, Export OFX 2.x, and Export QFX actions, and visible busy indicator | `BankReviewQueryService`; `ReviewedStatementAcceptanceService` over durable reviewed rows plus canonical `TransactionEntryService`; `BankStatementCsvExportService`/`BankStatementOfxExportService` | explicit one-row atomic acceptance through canonical transaction entry; deterministic atomic statement-file export; import writes remain in Import Preview | yes | yes for displayed review/match/accepted facts and selected export source | configured accounts, durable review facts, canonical accepted/matched transaction, `audit_event`, workspace-owned export coordinator | no automatic posting, batch/rule engine, static/session staging, selected-row compatibility serializer, ledger flattening, File-menu export, or generic job route | owner desktop verification of frozen preview, balanced acceptance, duplicate/protection guards, restart link, and non-posting import | P05/P15-S6/P15-S7/P15-S8/P16-S8 |
| `REPORT_LIBRARY` | `ReportLibraryPanel` | typed report list; report-specific as-of/range dates; All Funds/fund selector; conditional row limit; conditional stable-ID asset/item/control-account/status filters; Run, Export, Drill to Journal; split preview | `ReportExecutionService`, `FinancialReportService`, `SemanticAccountingReportQueryService`, `AssetInventoryReportQueryService`, semantic templates, lookup and preference services | file export and company-owned divider state | exported files and divider state yes | report data is queried from authoritative company-scoped H2 domain and canonical-ledger projections | typed report catalog/request/domain filter/result, core and specialized query services, semantic templates, export adapters | no selectable placeholders or second report ledger; S15 asset/inventory reports preserve exact differences, unlinked states, and canonical transaction IDs | owner P16-S15 desktop checklist | P11/P16-S13/P16-S15 |
| `CHART_OF_ACCOUNTS` | `ChartOfAccountsInterchangePanel` wrapping the existing `ChartOfAccountsPanel` | account table/editor New and Save; explicit Import JSON and Export JSON; import mode, mappings, preview, validation, confirmation, progress, and result details | account lookup/admin services plus `ChartOfAccountsJsonService` | existing `AccountAdminService` for single-account edits; `ChartOfAccountsJsonImportService` for one-transaction chart import; atomic deterministic file export | yes | yes | JPA chart/account model and durable `interchange_identity` | one account editor and one H2 authority; no parallel chart store or generic import job | owner desktop verification of preview, commit, export, dirty-state, and laptop-width behavior | P12/P15-S3 |
| `FUNDS` | `FundsPanel` | table, fields, fund type/status selectors, save/new | fund lookup/admin services | `FundAdminService` | yes | yes | JPA fund model | real admin write path exists | composition/validation hardening | P12 |
| `SETTINGS` | `AdministrationPanel` hosting `SettingsPanel`, `CompanyAdminPanel`, and `UserAdminPanel` | Preferences, Company Admin, and User Admin tabs; immediate theme; restart-labeled decoration/geometry choices; accounting defaults; company money/date formats; disabled explanations for privilege/reopen-scope compatibility values; read-only connected-database fact; H2 company lifecycle plus Users/Roles/Company Assignments maintenance | `UiSessionState`, production `AppStateStore`, executable `PreferenceConsumerMatrix`, `CompanyUiPreferencesService`, `CompanyAdminService`, `CompanySessionController`, `UserAdminService`; database changes route only through `DatabaseSessionController` | Save persists user-machine shell values through the workspace-owned store and company formats to H2; company lifecycle uses stable IDs; User Admin uses stable-ID user/role commands and active-company dated assignment/end/revoke commands with audit history; no Settings database-path write | yes | yes for company, user, global role definitions, company assignment history, audit events, and company UI preference data; shell convenience values intentionally use the user file | existing Administration hub, H2 `company`, `app_user`, `app_role`, `user_company_role`, `audit_event`, production workspace context, company UI state, `MainApp` shell lifecycle | connected database path is factual session state; User Admin records do not authenticate or enforce permissions; default privilege is not effective authorization; every enabled saved preference has a tested production consumer | owner P16-S16 user/role/assignment lifecycle and deferral checklist | P12/P16-S5/P16-S11/P16-S16 |
| `DIAGNOSTICS` | `DiagnosticsPanel` | Refresh Diagnostics; duplicate account/fund drill-through | typed `DiagnosticsQueryService.Report` over datasource, account/fund services, and active workspace facts | navigation only; database recovery uses typed commands on the Dashboard fallback | current runtime facts are queried on demand | H2 is authoritative for datasource/account/fund facts | lifecycle-owned service composition, `DatabaseSessionController`, typed `DatabaseRecoveryCommand` | no automatic repair and no generic job tracking; failures return an explicit unavailable report | P13-S2 desktop verification complete; retain in P14 route/lifecycle smoke coverage | P13/P14 |
| `HELP` | `HelpPanel` | scrollable current-workflow guidance and authoritative project links | static current navigation plus existing repository documents | external browser navigation only | n/a | n/a | canonical navigation contract and repository paths | browser-unavailable state exposes the copyable URL; no stale/nonexistent guide links | final P14 desktop sweep | P14 |

## Panels without `AppPanelId`

- `CompanyAdminPanel` and `UserAdminPanel` are hosted inside the stable `SETTINGS`/`AdministrationPanel` destination rather than assigned separate shell identifiers.
- `DashboardWorkspacePanel`, `DashboardPanelFX`, `ReferenceWorkspaceWindow`, and `DashboardExperiment` are alternate/reference dashboard/workspace components that P01 must classify or retire from the production shell.
- `DatabaseRecoveryPanel` is a recovery surface outside normal panel navigation. Its buttons dispatch `RETRY_CURRENT`, `SELECT_EXISTING`, and `CREATE_NEW` through the shell-owned typed recovery handler.

## Immediate backlog implications

1. P01 must replace static panel factories and global text dispatch with lifecycle-owned workspace services and typed commands.
2. P12 must remove remaining approval/rejection terminology from user-facing administration/audit surfaces unless the plan is amended.
3. P16-S9 completes financially relevant movement-to-ledger automation; inventory reporting remains later scope.
4. P06 owns per-line cleared-state resolution choices and reconciliation mutation; P16-S10 projects those authoritative facts into Journal without adding a Journal write path.
5. P16-S10 completes explicit line-level Journal cleared-state projection, including mixed multi-bank-line transactions and exact reconciliation drill-through.

## P15 governed interchange operation matrix

P15-S0 defines the contracts and fixtures. Whole-database transfer is implemented through P15-S2, Chart of Accounts JSON through P15-S3, and selected-company SCLX 1.3 export has its production File-menu route in P15-S4. P15-S5-C1 adds the production read-only SCLX preview route. P15-S5-C2 through C9 add each dependency-ordered canonical writer, and C10 completes correction relationships plus the guarded production commit route and semantic round-trip coverage. Standalone bank-statement exchange remains a later-slice delivery requirement.

| Exchange operation | User-visible scope | Recognition/query authority | Commit/write authority | Durable result | Explicitly not | Delivery slice |
|---|---|---|---|---|---|---|
| SCLX import/export | one selected active company's supported business data | content-root `SCLX` plus version 1.0/1.2/1.3; canonical H2 reads after company-ownership gate; preview uses one batched read-only target query | preview makes no writes; C2-C10 use one caller-owned transaction plus canonical services, including native correction restoration; P16-C9 preserves unrelated target operational history, reuses `IDENTICAL`, imports `NEW`, and applies explicit safe per-record target/source winners for `CONFLICT`; atomic SCLX 1.3 file export | preview shows exact counts, identities, create/reuse mappings, per-record conflict selectors, protected-choice explanations, target, SHA-256, and **Re-preview Same SCLX**; guarded UI commit reports commit/rollback and exact counts; export reports path/version/timestamp, counts, exclusions, bytes, and SHA-256 | database backup, COA-only file, bank statement, donor sidecar ledger, destructive replacement, cross-company reference rewrite, protected-history overwrite, or a partial import | P15-S4/P15-S5 complete; P16-C6, P16-C8, and P16-C9 existing-company corrections active |
| Chart of Accounts JSON | one chart and account hierarchy | actual donor compatibility fixture or `SCA-COA` 1.0 root through strict bounded parsing and non-mutating preview | `ChartOfAccountsJsonImportService` in one caller-owned transaction with parent-before-child writes and durable identities; atomic deterministic file export | counts, mappings, unsupported fields, exact paths, byte count, and SHA-256 | transaction history, SCLX, bank activity, database backup | P15-S3 implemented; owner desktop verification pending |
| Whole-database transfer | every company and H2-backed record | H2 backup structure/version and existing database diagnostics | supported H2 restore to a new path, migration, validation, then guarded session switch | backup manifest/result, validation report, SHA-256 | selected-company transfer, COA file, bank statement | P15-S2 |
| OFX 2.x/QFX/mapped CSV import | statement activity for one configured company-owned bank account | content-first secure XML/SGML/CSV profile parsing | one review-batch transaction to `bank_import_batch`, `bank_statement_line`, and `import_issue` | durable review batch, issues, exact/probable duplicate state, source SHA-256 | automatic `Txn`/`TxnSplit` creation, double-entry ledger import | P15-S6 |
| Normalized CSV and OFX/QFX statement export | durable selected-account statement activity | bank review query for one configured account/date range | atomic deterministic normalized CSV, OFX 2.x XML, or governed QFX 2.x statement file write; normalized CSV re-import uses the same durable review authority | counts, warnings, byte count, SHA-256; normalized CSV preserves governed identities/review facts; OFX/QFX preserves or discloses derived FITIDs and authoritative balances | canonical double-entry ledger export or automatic ledger creation | P15-S7 |

### P15 UI and command guardrails

- The four operation families require distinct labels, DTOs, previews, confirmations, results, and file chooser descriptions.
- The SCLX production File-menu action is labeled **Export Active Company to SCLX…** and its chooser is labeled **SCLX Active Company Files**; company activities, counterparties, merchants, payee references, line-level merchant references, canonical supplemental transaction details, bank configuration, reviewed statement/import facts, transaction-line clearance, reconciliation sessions/matches, fixed assets, and completed depreciation runs are exported, while remaining deferred sections are disclosed in the completion result rather than implied to be present.
- The production Import Preview actions are **Preview SCLX…**, **Apply SCLX Mappings**, and **Import Previewed SCLX…**. Preview captures the target company, runs away from the JavaFX thread, displays exact counts, identities, create/reuse mappings, protections, and path-coded messages, and states that no data changed. A target selector offers only compatible existing accounts/funds when a same-code collision needs resolution. Commit is enabled only for the retained exact nonblocking preview, with a nonblank actor, explicit approval of any displayed reuse mappings, and separate **Import into existing company (preserve settings)** consent when the populated target would change; confirmation names source, target, hash, count, preserved target settings, and atomic rollback behavior.
- `doc/data-exchange/shared-operation-contract.md` supplies only immutable lifecycle records, company-ownership validation, diagnostics, and external identity; it does not merge the four format authorities.
- Every selected-company preview fails closed while `company_ownership_issue` contains unresolved records relevant to the operation. Cross-company references are blocking errors, not warnings.
- SCLX preview renders each open ownership diagnostic as a blocking message with a durable resolution path. **Administration -> Company Ownership Diagnostics** supports confirmed, audited, one-record assignment of a direct ownerless row to the active import company; the selected target is authoritative, while actual cross-company reference conflicts remain non-assignable and are never rewritten.
- No top-level **Import/Export Jobs** destination or generic durable job queue returns.
- Current filename-based `BankDataEnvelopeRecognizer`, regex `OfxQfxTransactionExtractor`, per-row COA commit behavior, and direct `Files.writeString` paths are implementation evidence, not the final P15 contract.
- Bank import preview never implies that a canonical transaction has been created. Later acceptance/matching must remain a separate explicit canonical-service operation.
- Database restore never changes the selected path until the restored copy passes migration, schema validation, diagnostics, and clean reopen.
